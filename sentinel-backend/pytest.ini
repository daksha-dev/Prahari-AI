[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    sarvam_real: mark test as making real Sarvam API calls
addopts = -v --tb=short -m "not sarvam_real"
